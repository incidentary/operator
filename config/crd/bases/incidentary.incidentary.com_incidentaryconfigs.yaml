---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: incidentaryconfigs.incidentary.incidentary.com
spec:
  group: incidentary.incidentary.com
  names:
    kind: IncidentaryConfig
    listKind: IncidentaryConfigList
    plural: incidentaryconfigs
    shortNames:
    - inc
    singular: incidentaryconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.watchedWorkloads
      name: Watched
      type: integer
    - jsonPath: .status.matchedServices
      name: Matched
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IncidentaryConfig is the Schema for the incidentaryconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of IncidentaryConfig
            properties:
              apiKeySecretRef:
                description: |-
                  apiKeySecretRef references the Kubernetes Secret containing the
                  Incidentary workspace API key.
                properties:
                  key:
                    description: key is the entry within the Secret data that holds
                      the value.
                    type: string
                  name:
                    description: name is the Secret name.
                    type: string
                required:
                - key
                - name
                type: object
              eventFilters:
                description: eventFilters controls which K8s events are sent to Incidentary.
                properties:
                  minSeverity:
                    default: warning
                    description: |-
                      minSeverity is the lowest severity level that will be sent to Incidentary.
                      Tier 1 events (crashes, OOMs, failures, deploys) always pass regardless of this threshold.
                    enum:
                    - trace
                    - debug
                    - info
                    - warning
                    - error
                    - fatal
                    type: string
                type: object
              excludeNamespaces:
                description: |-
                  excludeNamespaces is a list of namespaces the operator will not watch.
                  Defaults to: kube-system, kube-public, kube-node-lease.
                items:
                  type: string
                type: array
              ingestEndpoint:
                description: |-
                  ingestEndpoint overrides the default Incidentary ingest URL.
                  Default: https://api.incidentary.com/api/v2/ingest
                type: string
              reconciliationIntervalSeconds:
                default: 300
                description: |-
                  reconciliationIntervalSeconds is how often the operator re-discovers
                  workloads and sends topology reports.
                format: int32
                maximum: 3600
                minimum: 30
                type: integer
              topologyEndpoint:
                description: |-
                  topologyEndpoint overrides the default Incidentary topology URL.
                  Default: https://api.incidentary.com/api/v2/workspace/topology
                type: string
              watchNamespaces:
                description: |-
                  watchNamespaces restricts the operator to these namespaces.
                  When empty (default), the operator watches all namespaces except those
                  listed in excludeNamespaces.
                items:
                  type: string
                type: array
            required:
            - apiKeySecretRef
            type: object
          status:
            description: status defines the observed state of IncidentaryConfig
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the IncidentaryConfig resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Ready": the operator has successfully reconciled the config and is watching resources

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconciliation:
                description: lastReconciliation is the timestamp of the most recent
                  successful reconciliation cycle.
                format: date-time
                type: string
              matchedServices:
                description: matchedServices is the count of watched workloads that
                  match an SDK-registered service.
                format: int32
                type: integer
              phase:
                description: 'phase is a high-level summary: Running, Degraded, Error,
                  or empty (not yet reconciled).'
                type: string
              unmatchedWorkloads:
                description: unmatchedWorkloads is the count of watched workloads
                  that do not match any registered service.
                format: int32
                type: integer
              watchedWorkloads:
                description: watchedWorkloads is the count of Deployments/StatefulSets/DaemonSets
                  currently tracked.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
